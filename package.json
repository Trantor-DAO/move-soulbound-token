{
  "name": "trantor-sui",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "execute:query": "ts-node scripts/query.ts",
    "execute:createCollection": "ts-node scripts/createCollection.ts",
    "execute:claim": "ts-node scripts/claim.ts",
    "build": "cd move_packages/sui-sbt && sui move build",
    "test": "cd move_packages/sui-sbt && sui move test"
  },
  "dependencies": {
    "@mysten/sui.js": "^0.26.1",
    "dotenv": "^16.0.2"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
